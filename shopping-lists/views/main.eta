<% layout("./layouts/layout.eta") %>

<h1> Statistics :</h1>
<% if (it.listCount.rows.length === 0) { %>
    <h2> No shopping lists yet. </h2>
<% } else { %>
    <h2> Shopping lists: <%= it.listCount.rows.length %> </h2>
    <h2> Shopping list items: <%= it.itemCount.rows.length %> </h2>
<% } %>

<a href="/lists"> Lists </a>